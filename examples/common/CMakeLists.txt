#==============================================================================
# Common utilities for VneEvents examples
#==============================================================================

# This directory contains shared utilities used by examples.
# Currently, it only contains header files, so no library target is needed.
# The headers are included via target_include_directories in individual examples.
